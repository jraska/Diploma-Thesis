<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextMain">
        <item name="android:textColor">@color/textMain</item>
        <item name="android:textSize">@dimen/textMain</item>
    </style>

    <style name="TextMainInverse">
        <item name="android:textColor">@color/textMainInverse</item>
        <item name="android:textSize">@dimen/textMain</item>
    </style>

    <style name="TextSecondary">
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:textSize">@dimen/textSecondary</item>
    </style>

    <style name="TextInputMain" parent="TextMain">
        <item name="android:textColorHint">@color/textSecondary</item>
        <item name="android:textColorHighlight">@color/colorPrimary</item>
    </style>

    <style name="IconButton">
        <item name="android:padding">@dimen/padding_large</item>
    </style>

    This is here to fix bug with inflating
    <style name="PopupMenuCompat" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:popupBackground">#0F213F</item>
        <item name="android:disabledAlpha">0.5</item>
    </style>

</resources>
